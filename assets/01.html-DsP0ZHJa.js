import{_ as a,c as n,a as i,b as e,d as t,o}from"./app-CCsZbG-M.js";const r={};function h(l,s){return o(),n("div",null,s[0]||(s[0]=[i('<p>As you study this section, answer the following questions:</p><ul><li>What is the purpose of the shell declaration (the shebang)?</li><li>How can you comment out lines in a shell script?</li><li>Which methods can you use to run a script from the command-line?</li><li>Why might you need to use the <b class="fw-bold">declare</b> command in a script?</li></ul><p>In this lesson, you will learn to:</p><ul><li>Execute and source a script</li></ul><p>Key terms for this section include the following:</p><table class="terms"><thead><tr><th>Term</th><th>Definition</th></tr></thead><tbody><tr><td>Shell declaration</td><td> The first line in a script that holds the path to the bash interpreter. </td></tr><tr><td>Comment</td><td> A line in a bash shell script that helps a script writer understand how the script is constructed but is ignored when the script is run. </td></tr><tr><td>Variable</td><td>A key-value pair that maps a variable name to a value.</td></tr></tbody></table><p>This section helps you prepare for the following certification exam objectives:</p><table class="objectives"><thead><tr><th>Exam</th><th>Objective</th></tr></thead><tbody><tr><td>CompTIA Linux+ XK0-005</td><td> 3.1 Given a scenario, create simple shell scripts to automate common tasks <br><ul><li>Shell script elements</li><ul><li>Variables</li><li>Shell built-in commands</li><ul><li>Read</li><li>echo</li><li>source</li></ul></ul><li> Environment variables <ul><li>$PATH</li></ul></li><li>Relative and absolute paths</li></ul></td></tr></tbody></table><h2 id="_14-1-1-bash-scripting-overview" tabindex="-1"><a class="header-anchor" href="#_14-1-1-bash-scripting-overview"><span>14.1.1 Bash Scripting Overview</span></a></h2><p>Click one of the buttons to take you to that part of the video.</p><p>Bash Scripting Overview 00:00-00:25 In this lesson, we&#39;ll discuss bash scripting. Linux allows you to create powerful shell scripts that can be run from the shell prompt. Shell scripts can be used to run single commands or multiple commands sequentially. They can also be used to read input from the end user or other shell commands and make decisions based on that input.</p><p>Sample Shell Script 00:25-00:36 A shell script is a text file that contains a series of commands that are executed in the order they appear in the file. A sample shell script is shown here.</p><p>Specify Shell 00:36-01:02 First is the pound sign (#), an exclamation point (!), and then the path to the bash shell. The first line of any shell script must specify which shell the script is written to run under. Here we&#39;re going to run this using the &#39;/bin/bash&#39; shell. When the script is run, a new subshell is created using the specified shell, and then the script&#39;s contents are processed within that sub-shell.</p><p>Script Comments 01:02-01:22 Next, here&#39;s a comment. Notice there&#39;s a pound sign followed by some text. A line of text within a script that begins with a pound sign is just a comment that won&#39;t be displayed or processed. Comments are optional, but it&#39;s best to comment right after the shell declaration with a description of what the script does and sometimes who wrote it.</p><p>Shell Commands 01:22-02:00 Next, we have two commands, the echo command and the date command. These elements are simple shell commands we normally use at the shell prompt, and we&#39;ll run them from within the script.</p><p>First is the echo command, which we&#39;ll use to display this text on the screen. Then, when that command is done, it will run the date command, displaying the current date and time on the screen. These are optional. You can choose whatever commands you want inside the shell script.</p><p>The last part is &#39;exit 0.&#39; This indicates the end of the script and tells the shell what to do after running the commands in the script. This is telling the shell to exit.</p><p>Run the Script 02:00-02:34 When we run the script, the output is sent to the standard out, which by default, is the screen. Notice that the script first displayed the text indicated by the echo command. Then, the date command was processed. The output is shown here. Then the shell exited the script and returned us to the command prompt as it was scripted. Shell scripting is very flexible. Just remember to put each command on a separate line. This was a simple script, with no branching, decision-making within the script, or end-user interaction.</p><p>Capture User Input 02:34-03:07 The bash shell allows for much more complex scripts than this simple one. For example, we could make the script interactive by having the script ask the user a question and capture the input for processing. This is typically done using the echo command with the read command.</p><p>The echo command is used to present the user with a question. Here it&#39;s, &quot;What directory do you want to add to the path?&quot; The read command is used to pause the script, present a prompt on the screen, and then read in the input that the user enters in a variable we define.</p><p>The read Command 03:07-04:07 In this script, the user is prompted for the name of a directory he or she wants to add to the path environment variable. The read command here provides the user with a prompt to enter a directory name. Once the user presses the Enter key, the value that was inputted is assigned to the variable, here, named NEWPATH.</p><p>The variable is stored in memory, and the echo command is used again to display the value they entered, which says, &quot;You want to add,&quot; and then we reference the NEWPATH variable.</p><p>Here we asked the user to specify a directory path to be added to the path environment table, but we didn&#39;t actually add it in this script. We just echoed back, &quot;This is the path you want to add,&quot; and then we ended the script.</p><p>If we want to modify the path environment table variable, we need to add more commands to the shell script. It&#39;s helpful when writing scripts to ask, &quot;If I were doing this from the shell prompt, what commands would I use?&quot; Then, enter those commands in the script.</p><p>Apply User Input 04:07-04:43 In this example, we&#39;ll use the same script, except we&#39;ll add the directory specified by the user to the path environment table and then export the path. Notice that we&#39;ve added the variable assignment command to the script where we say &quot;PATH=.&quot; Then we include whatever the current paths are in the path environment variable and then append to the end of those paths a colon (😃 followed by the path that the end user specified.</p><p>Then we export it and use the echo command to show the end user what the path environment variable looks like. Here it&#39;s, &quot;Your path environment variable is now:&quot; followed by the current value.</p><p>Dynamic Variable Declaration 04:43-05:05 You may have noticed that we didn&#39;t declare the NEWPATH variable anywhere in the script. Many other scripting or programming languages do require this. The bash shell&#39;s a lot more forgiving. In this instance, the bash shell will create the new path variable for us in memory dynamically. Then, using the read command, it will assign whatever the user inputs into NEWPATH.</p><p>Declare Variable Type 05:05-06:01 Of course, you can declare and type variables within the script if you want to. This is done using the declare command in the script. This could be useful when you want the user to enter a number instead of text with the read command.</p><p>Remember, when creating shell scripts, the bash shell interprets anything entered in at the read command by the user as text, even if it contains numbers. Fortunately, if we declare the variables first and type them as integers, the variable will be interpreted as numbers on which you can perform arithmetic operations. You can&#39;t do that with text.</p><p>Here, we&#39;ve added declare statements to the beginning of a script to declare three variables named NUM1, NUM2, and TOT. Because we used the -i option, the variables are specified as integers. So, the bash shell will interpret the values assigned to these variables as whole numbers. This allows us to perform any arithmetic operations we want on them.</p><p>Summary 06:01-06:28 That&#39;s it for this lesson. In this lesson, we introduced you to basic scripting concepts on a Linux system. We talked about the components that comprise the shell script. We then discussed how you can use the read command to read input from the end user in a script. And then, we ended this lesson by reviewing how to declare and type variables within a script.</p><h2 id="_14-1-2-bash-script-execution" tabindex="-1"><a class="header-anchor" href="#_14-1-2-bash-script-execution"><span>14.1.2 Bash Script Execution</span></a></h2><p>Click one of the buttons to take you to that part of the video.</p><p>Bash Script Execution 00:00-00:20 When developing scripts, we usually want to execute them from the shell prompt. Unfortunately, our scripts aren&#39;t automatically configured to work this way. In this lesson, we&#39;ll take a look at some options for running shell scripts and how to properly configure the PATH variable so that they can run without errors.</p><p>Options for Running Shell Scripts 00:20-01:22 When creating our scripts, we start out by writing them and saving them as a text document. If you try to run a newly created script file, you&#39;ll get an error message because new files aren&#39;t executable by default. To get around this, we have two options.</p><p>The first is to simply call the shell executable itself, such as /bin/bash, and tell it to execute the script file.</p><p>The other option is to assign the Execute permission to the text file. With the Execute permission assigned, any file—including a simple text file—can execute from the shell prompt. To make this happen, you simply enable the Execute permission for the owner, group, others, or any combination of them using the chmod command. So in this example here, we&#39;ve added the Execute permission to the owner on this file. We can do the same thing using the numeric permission format as seen here.</p><p>After that, I can run this script from the shell prompt. All I have to do is enter its name, just as I would with any other system command.</p><p>Configure PATH 01:22-04:36 But there&#39;s one other problem that you&#39;ll likely run into when using scripts—the path configuration.</p><p>When running a script, the Bash shell needs to know where the file exists. If we&#39;re just running our own scripts for some testing, we need to either specify the full path to the script file or use a relative path. Even if you&#39;re in the same directory, if you don&#39;t specify a full or relative path to the script file, you&#39;ll get a Command Not Found error message. By supplying the full or relative path, the Bash shell knows where the script is and doesn&#39;t have to look through the PATH environment variable to find it.</p><p>If an administrator wants others to run their scripts, they&#39;ll typically want to avoid having other users type the full or relative path. One option is to save the script in a directory that already exists in the PATH [path]environment variable—maybe in the bin directory within each user&#39;s home directory. Most Linux distributions automatically include this in the PATH variable, as long as the bin directory exists somewhere. A Bash configuration file automatically adds that directory to the PATH environment variable when the shell is started. Then, because it&#39;s there, any stored files can be run from the shell prompt without specifying the full path to the file.</p><p>Alternatively, you could take the directory where that script file resides and add it to the existing PATH environment variable. To do this, we need to use two different commands. First, we use PATH= to set the value of the PATH environment variable. Notice that we first reference the existing PATH environment variable with $PATH.</p><p>If we don&#39;t do this, we&#39;ll overwrite PATH, and nothing will work properly on the system afterwards. Then we append a colon followed by the new directory path that we want to add. The last step is to export the new PATH variable. By exporting it, we make sure that the newly assigned value will be made available to all other shell sessions. If we don&#39;t export it, the new variable will only apply to the current one.</p><p>Unfortunately, when the system reboots, the PATH variable changes won&#39;t be saved and will reset to the default. To make sure that the new settings are persistent across system restarts, we need to edit one of the Bash configuration files and add these two variable assignment commands to that file.</p><p>At this point, you should decide which shell configuration file is the right one to use. It depends on a few things. First, it depends on your Linux distribution because different distributions use different file sets. You have to look at your distribution to figure out which one it uses.</p><p>Do you want it to apply to just one user on the system—maybe your user account, for example? Or do you want this change to apply to all users? If you want this to apply to everyone, you need to make the change to a global Bash configuration file, such as /etc/profile. On the other hand, if you only cared about making the change for a single user—such as yourself—you&#39;d just find the appropriate hidden configuration file within your /home directory. It might be something like.bashrc. Then it&#39;d only apply to your particular user account.</p><p>Okay, that&#39;ll wrap up this lesson on Bash script execution.</p><p>Summary 04:36-05:01 In this lesson, we went over some options for running shell scripts, like calling the shell executable itself or assigning the Execute permission to the text file. We also went over some different problems that could arise with the PATH variable and how to address these concerns.</p><h2 id="_14-1-3-executing-and-sourcing-a-script" tabindex="-1"><a class="header-anchor" href="#_14-1-3-executing-and-sourcing-a-script"><span>14.1.3 Executing and Sourcing a Script</span></a></h2><p>Click one of the buttons to take you to that part of the video.</p><p>Execute and Source a Script 00:00-00:34 In this demonstration, we&#39;re going to look at the differences between executing a script and sourcing a script. The main difference between the two is that when you execute a script, it runs in a new shell process. When you source a script, it still runs, but it&#39;s run in your current shell process. The important thing to understand about execution is that since the script is run in a new shell process, any environment changes made while the script is being executed will be gone once the script finishes running.</p><p>Create Scripts 00:34-01:50 To see how this works, let&#39;s use nano to create a new script called sourced.sh. The first entry I&#39;ll add is the simple command, echo $$. This command displays the current process IDs that are being used in my current shell.</p><p>Next, I&#39;ll create a variable by typing A=&quot;This script is sourced&quot;. And then, to be able to see the contents of that variable when the script runs, I&#39;ll add echo $A. To save my changes, I press Ctrl + O to commit the script to a disk, and then I press Enter to use the name that I specified when nano was started.</p><p>To create a second script, let&#39;s change the variable from A to B and change the sentence to say executed instead of sourced. And lastly, let&#39;s change the second echo line to reflect our new variable. To save these changes as a new file, I&#39;ll press Ctrl+X and then Y to confirm my choice. But now I want to change the name to executed.sh and then press Enter. And since I changed the name, I need to type Y one more time to confirm the new name.</p><p>Change Permissions 01:50-02:45 Now when I run ls -l *.sh, you see that the two scripts were created. But when you look at the permissions, you see that neither have the Execute permission. That means as they now stand, I won&#39;t be able to run them.</p><p>To grant the Execute permission, I first type chmod. Then for the permissions, I&#39;ll type u+x and the names of the two files that I want to change, which are sourced.sh and executed.sh. The u+x in this command means that we&#39;re going to add the Execute permission for the owner of this script.</p><p>Now when I run ls -l *.sh again, you can see that the owner of these two scripts now has the appropriate permission. Also notice that the files have changed to the color green, which for this distribution means that they&#39;re executable files.</p><p>Source a Script 02:45-03:57 With our scripts created, let&#39;s run them and see what happens. Let&#39;s start with the sourced script. To help you remember what this script does, I&#39;ll run cat sourced.sh here. And then, to be able to see my current shell&#39;s process ID, I&#39;ll run the ps command.</p><p>To source a script, you can either type source and the script&#39;s name, or you can type . and the script&#39;s name. Know that if you use the source option, the script will run if it&#39;s in the current directory. If not, you can find it by looking in the $PATH variable. In other words, when you use just a dot, the script has to exist in the current directory.</p><p>Notice that when I press Enter, the echo $$ line of our script shows the same process ID as my Bash shell, indicating that it was run in my current shell. It also created the A environment variable, and the contents of that variable were echoed to the screen. Since a sourced script runs in your current shell, any environment variables are still available even after the script is run. To prove that, when I run echo $A outside of the script, I still see the contents of the A variable.</p><p>Execute a Script 03:57-04:47 To better see the difference between sourcing and execution, let&#39;s clear the screen and repeat this process, but this time we&#39;ll run our second script. First, we&#39;ll use cat to display it. Next, we&#39;ll run ps to see the current processes. And now, we&#39;ll execute the script by running ./executed.sh.</p><p>As we examine the results, notice that the Process number used to run the script is different from the current Bash shell. With this script, we created the B environment variable, but when I run echo $B, all we see is the new line that the echo command printed. That&#39;s because, as we told you earlier, when you execute a script, any changes to the environment are lost once the script has been run. That&#39;s why there isn&#39;t anything currently stored in our B variable.</p><p>Summary 04:47-04:59 And that&#39;ll be it for our demonstration. In this demo, we showed you the differences between executing and sourcing a script.</p><h2 id="_14-1-4-scripting-facts" tabindex="-1"><a class="header-anchor" href="#_14-1-4-scripting-facts"><span>14.1.4 Scripting Facts</span></a></h2><p>At its most basic level, a Bash script is a set of commands stored in a file. When the Bash shell reads the file, it executes the commands as if they were typed on the keyboard.</p><p>This lesson covers the following topics:</p><ul><li>Basic bash script rules and components</li><li>The time command</li></ul><h3 id="basic-bash-script-rules-and-components" tabindex="-1"><a class="header-anchor" href="#basic-bash-script-rules-and-components"><span>Basic Bash Script Rules and Components</span></a></h3><p>There are basic rules that govern how a Bash script should be written and run.</p><ul><li>Specify the Bash shell that you intend to run the script.</li><li>Use comments to explain what the script does.</li><li>Use <b class="fw-bold">exit 0</b> to end the script.</li><li> Assign execute permissions to the script with the <b class="fw-bold">chmod</b> command. </li><li> Use one of the following methods to run the script: <ul><li> Add the folder that contains the script to the <b class="fw-bold">PATH</b> environment variable; then enter the script name at the shell prompt. </li><li> Save the script in a folder that is already in <b class="fw-bold">PATH</b> , such as /usr/bin or /bin; then enter the name of the script. </li><li> Type the full path name to the script to run it from anywhere. </li><li> Type <b class="fw-bold">./ <i class="fs-italicize">script_name</i></b> to run the script if it resides in the current directory. (./ indicates the present working directory.) </li></ul></li></ul><p>The following table lists some simple scripting components:</p>',71),e("table",null,[e("thead",null,[e("tr",null,[e("th",null,"Component"),e("th",null,"Description"),e("th",null,"Examples")])]),e("tbody",null,[e("tr",null,[e("td",null,"Shell declaration"),e("td",null," The shell declaration should come first in the script. It starts with a number sign and exclamation point (#!), followed by the path to the shell executable. A common nickname for this line is shebang. "),e("td",null,[e("b",null,"#!/bin/bash"),t(" specifies that the script will run in the Bash shell. ")])]),e("tr",null,[e("td",{height:"74"},"Comments"),e("td",{height:"74"}," Comments begin with a number sign (#). The shell ignores these lines when running the script. Comments help communicate how the script was constructed and what it is designed to do. "),e("td",{height:"74"},[e("b",null,"# This is a Bash script")])]),e("tr",null,[e("td",null,"Commands"),e("td",null,[t(" When a script runs, it executes commands as if they were entered in the command line. Commands can be typed on a single line or separated using a semi-colon (;). "),e("h2",null,"echo"),e("ul",null,[e("li",null," It is a good practice to always use quotes when you want a variable to represent a literal value. "),e("li",null,"Use a dollar sign ($) to display the value of a variable."),e("li",null,"Use a backslash (\\) to display special characters.")]),e("p",null,[t(" The "),e("b",null,"read"),t(" command creates a variable and prompts the user to type in text. It assigns the value the user types to the variable. By default, the user input is treated as a text string. ")])]),e("td",null,[e("b",null,[t("#!/bin/bash"),e("br"),t(" ls /home/user/Pictures "),e("br"),t(" exit 0")]),e("p",null,[t(" This script uses the "),e("b",null,"ls"),t(" command to list the contents of the /home/user/Pictures directory. ")]),e("b",null,[t("#!/bin/bash "),e("br"),t(' echo "What is your name?" '),e("br"),t(" read variable1 "),e("br"),t(' echo "Hello," $variable1"."'),e("br"),t(" exit 0")]),e("br"),e("p",null,[t(" The script prints What is your name? on the screen, prompts the user for input, captures the user's input in a variable named variable1, then displays the contents of that variable on the screen "),e("i",{class:"fs-italicize"},".")])])]),e("tr",null,[e("td",{height:"245"},"Variables"),e("td",{height:"245"},[t(" Variables hold values that the script uses when running. These values can be either numbers or text. Keep the following in mind when using variables: "),e("ul",null,[e("li",null," Linux script variables are commonly written using all capital letters. This helps programmers quickly identify them. "),e("li",null," When creating variables, place the equals sign (=) immediately after the variable with no space. If a space follows the variable name, the script treats it as a command and tries to execute it. "),e("li",null," Use a space after the equals sign (=) only when you want the variable to be the output of a command. "),e("li",null," Use quotes if a variable value has a space in it. It is a good practice to always use quotes when you want a variable to represent a character string. ")])]),e("td",{height:"245"},[e("b",null,"variable1=Hello"),t(" assigns variable1 the value of Hello. "),e("br"),e("b",null,"variable1 = Hello"),t(" causes an error because the script tries to run the command variable1, which by default does not exist. "),e("br"),e("b",null,'variable1="Hello, Mr. Smith"'),t(" assigns variable1 the value of Hello, Mr. Smith. "),e("br"),e("b",null,"variable1=Hello, Mr. Smith"),t(" assigns variable1 the value of Hello, and then displays an error because it treats Mr. as a command and tries to execute it. "),e("br"),e("b",null,"variable1=pwd"),t(" assigns variable1 the value of pwd. "),e("br"),e("b",null,"variable1= pwd"),t(" assigns variable1 the value of the result of running the "),e("b",null,"pwd"),t(" command. (For example, /home/jdoe.) "),e("div",null,[e("div",{class:"to-info-box"},[e("div",{class:"to-info-box-body"},[e("div",{class:"to-icon large","aria-hidden":"true",style:{width:"20px"}},[e("svg",{"aria-hidden":"true",focusable:"false","data-prefix":"fal","data-icon":"circle-info",class:"svg-inline--fa fa-circle-info",role:"img",xmlns:"http://www.w3.org/2000/svg",viewBox:"0 0 512 512"},[e("path",{fill:"currentColor",d:"M256 32a224 224 0 1 1 0 448 224 224 0 1 1 0-448zm0 480A256 256 0 1 0 256 0a256 256 0 1 0 0 512zM208 352c-8.8 0-16 7.2-16 16s7.2 16 16 16l96 0c8.8 0 16-7.2 16-16s-7.2-16-16-16l-32 0 0-112c0-8.8-7.2-16-16-16l-40 0c-8.8 0-16 7.2-16 16s7.2 16 16 16l24 0 0 96-32 0zm48-168a24 24 0 1 0 0-48 24 24 0 1 0 0 48z"})])]),e("div",{class:"to-info-box-body-content"},[e("span",null,[t(" You can manipulate environment variables from within a script. For example, you could modify the MAILTO environment variable by including "),e("b",null,"MAILTO=root"),t(" in a script. This will cause notifications or other conditional events to be mailed to the root superuser account by default.")])])])])])])]),e("tr",null,[e("td",null,"Variable declarations"),e("td",null,[t(" The "),e("b",null,"declare -i"),t(" command is used to type a variable as an integer. It can only contain whole numbers. It can't contain text or numbers with decimal places. "),e("br"),t(" The "),e("b",null,"declare -f"),t(" command is used to display all defined functions or just a specific defined function. ")]),e("td",null,[e("h2",null," #!/bin/bash declare -i num1 declare -i num2 declare -i total num1=7 num2=5 total=num1+numw echo $total exit 0 "),t(" The output of this script is the integer 12 because the shell treats the variable values as integers. "),e("div",null,[e("div",{class:"to-info-box"},[e("div",{class:"to-info-box-body"},[e("div",{class:"to-icon large","aria-hidden":"true",style:{width:"20px"}},[e("svg",{"aria-hidden":"true",focusable:"false","data-prefix":"fal","data-icon":"circle-info",class:"svg-inline--fa fa-circle-info",role:"img",xmlns:"http://www.w3.org/2000/svg",viewBox:"0 0 512 512"},[e("path",{fill:"currentColor",d:"M256 32a224 224 0 1 1 0 448 224 224 0 1 1 0-448zm0 480A256 256 0 1 0 256 0a256 256 0 1 0 0 512zM208 352c-8.8 0-16 7.2-16 16s7.2 16 16 16l96 0c8.8 0 16-7.2 16-16s-7.2-16-16-16l-32 0 0-112c0-8.8-7.2-16-16-16l-40 0c-8.8 0-16 7.2-16 16s7.2 16 16 16l24 0 0 96-32 0zm48-168a24 24 0 1 0 0-48 24 24 0 1 0 0 48z"})])]),e("div",{class:"to-info-box-body-content"},[e("span",null,[t(" Numeric text strings can also be converted to integers using the following format: "),e("br"),e("b",null,"echo $[$num1+$num2]"),t(" gives an output of 12 regardless of whether the "),e("b",null,"declare"),t(" command is used.")])])])])])])])])],-1),i('<h3 id="the-time-command" tabindex="-1"><a class="header-anchor" href="#the-time-command"><span>The time Command</span></a></h3><h4 id="time" tabindex="-1"><a class="header-anchor" href="#time"><span>time</span></a></h4><table><thead><tr><th>Command</th><th>Description</th><th>Examples</th></tr></thead><tbody><tr><td><b>time <i>command</i></b></td><td> The output shows three values as follows: <ul><li> real - The time from the moment the Enter key was pressed until the moment the command is completed. </li><li>user - The amount of CPU time spent in user mode.</li><li>system - The amount of CPU time spent in kernel mode.</li></ul></td><td><b>time wget wget https://wordpress.org/latest.zip</b> use the <b>wget</b> command to download the zip file, but also displays the time it took. <h2> real     0m1.604s user    0m0.042s sys      0m0.099s </h2></td></tr></tbody></table>',3)]))}const d=a(r,[["render",h],["__file","01.html.vue"]]),u=JSON.parse('{"path":"/14/01.html","title":"Section 14.1 Bash Shell Scripting","lang":"zh-CN","frontmatter":{"lang":"zh-CN","title":"Section 14.1 Bash Shell Scripting","description":"some description"},"headers":[{"level":2,"title":"14.1.1 Bash Scripting Overview","slug":"_14-1-1-bash-scripting-overview","link":"#_14-1-1-bash-scripting-overview","children":[]},{"level":2,"title":"14.1.2 Bash Script Execution","slug":"_14-1-2-bash-script-execution","link":"#_14-1-2-bash-script-execution","children":[]},{"level":2,"title":"14.1.3 Executing and Sourcing a Script","slug":"_14-1-3-executing-and-sourcing-a-script","link":"#_14-1-3-executing-and-sourcing-a-script","children":[]},{"level":2,"title":"14.1.4 Scripting Facts","slug":"_14-1-4-scripting-facts","link":"#_14-1-4-scripting-facts","children":[{"level":3,"title":"Basic Bash Script Rules and Components","slug":"basic-bash-script-rules-and-components","link":"#basic-bash-script-rules-and-components","children":[]},{"level":3,"title":"The time Command","slug":"the-time-command","link":"#the-time-command","children":[]}]}],"git":{"updatedTime":1736831526000},"filePathRelative":"14/01.md"}');export{d as comp,u as data};
